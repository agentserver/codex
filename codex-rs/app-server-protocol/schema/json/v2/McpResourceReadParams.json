{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "environmentId": {
      "description": "Optional environment used to read the resource. For thread-scoped requests this must resolve to the thread's selected primary environment until per-environment MCP managers are available.",
      "type": [
        "string",
        "null"
      ]
    },
    "server": {
      "type": "string"
    },
    "threadId": {
      "type": [
        "string",
        "null"
      ]
    },
    "uri": {
      "type": "string"
    }
  },
  "required": [
    "server",
    "uri"
  ],
  "title": "McpResourceReadParams",
  "type": "object"
}